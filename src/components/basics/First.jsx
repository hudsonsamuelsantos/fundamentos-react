import React from "react"

const message = 'Seja bem vindo ao React'

export default function First() {
    return (
        <div>
            <h2>Primeiro componente</h2>
            <p>{message}</p>
        </div>
    )
}